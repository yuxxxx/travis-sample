language: ruby
rvm: "2.1.0"
before_install:
  - 'rspec'
  - 'bundler'
script: "bundle && bundle exec rspec"
notifications:
  emails:
    - "yuunnxx@gmail.com"
  on_success: always
  on_failure: always